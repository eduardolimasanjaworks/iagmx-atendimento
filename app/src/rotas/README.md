# Rotas

Entradas HTTP do IAGMX.
Cada rota deve delegar a regra de negocio para `servicos` e manter validacao enxuta.
Preferencia por arquivos pequenos e endpoints admin separados dos fluxos publicos.
